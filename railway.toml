[build]
builder = "nixpacks"
buildCommand = "./build.sh"

[deploy]
startCommand = "cd Home && gunicorn Home.wsgi:application --bind 0.0.0.0:$PORT --workers 1"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
